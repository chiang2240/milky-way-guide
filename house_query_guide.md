# 房價查詢助手 AI — 查詢教學手冊

> 適用平台：內政部實價登錄開放資料 API  
> 最後更新：2026-05-08

---

## 目錄

1. [開始前準備](#開始前準備)
2. [場景一：查詢特定行政區近期成交行情](#場景一查詢特定行政區近期成交行情)
3. [場景二：比較同區域不同屋型的價差](#場景二比較同區域不同屋型的價差)
4. [場景三：查詢預售屋成交資訊](#場景三查詢預售屋成交資訊)
5. [場景四：租賃行情查詢](#場景四租賃行情查詢)
6. [API 呼叫範例](#api-呼叫範例)
7. [常見問題排除](#常見問題排除)
8. [查詢速查表](#查詢速查表)

---

## 開始前準備

### 取得 API 金鑰

1. 前往 [政府資料開放平台](https://data.gov.tw) 註冊帳號
2. 登入後至「會員中心」→「API 金鑰管理」申請金鑰
3. 金鑰格式：32 位英數字串，例如 `a1b2c3d4e5f6...`
4. 將金鑰存入環境變數，不要寫死在程式碼中：

```bash
export DATA_GOV_API_KEY="你的金鑰"
```

### 確認查詢目標

出發查詢前先確認：

| 項目 | 建議 |
|------|------|
| 目標縣市 | 確認縣市代碼（見 [API 參數清單](house_api_reference.md)） |
| 目標行政區 | 精確到鄉鎮市區，避免資料量過大 |
| 時間範圍 | 通常查近 4 季（1 年）即可反映現況 |
| 交易類型 | 買賣 / 預售 / 租賃，目的不同選不同端點 |
| 建物型態 | 大樓、公寓、透天等，過濾比較才有意義 |

---

## 場景一：查詢特定行政區近期成交行情

### 適用情境

想知道某個行政區最近一年內的實際成交行情，作為購屋或估價參考。

### 查詢步驟

1. 確認縣市代碼（例：台北市 = `A`）
2. 決定查詢年季範圍（例：近四季 = 1131 ~ 1134）
3. 發出 API 請求，取回資料
4. 依行政區篩選，剔除異常值
5. 計算平均單價、中位數總價

### 關鍵參數設定

| 參數 | 設定值 | 說明 |
|------|--------|------|
| `county` | `A`（台北市）| 目標縣市 |
| `season` | `1134` | 最新一季 |
| `district` | `大安區` | 指定行政區 |
| `type` | `buy` | 買賣成交 |
| `format` | `json` | 回傳格式 |

### 資料清洗步驟

取回資料後，以下幾類資料建議**過濾排除**：

| 排除條件 | 原因 |
|----------|------|
| 總價低於 50 萬 | 可能為車位單獨交易 |
| 總價高於市場均價 3 倍以上 | 可能為整棟買賣或資料異常 |
| 建物面積 < 5 坪 | 非一般住宅 |
| 備註欄含「特殊關係」| 親屬交易，不反映市場行情 |
| 備註欄含「其他非一般性交易」| 法拍、贈與等特殊交易 |

---

## 場景二：比較同區域不同屋型的價差

### 適用情境

同一個行政區，公寓、大樓、透天的單價差距有多大？

### 查詢邏輯

```
同縣市 + 同行政區 + 同時間範圍
→ 依「建物型態」分組
→ 各組計算平均單價（元/坪）
→ 輸出比較表
```

### 建物型態分組建議

| 分組 | 包含型態 | 說明 |
|------|----------|------|
| 大樓類 | 住宅大樓（11F+）、華廈（≤10F）| 有電梯，公設比高 |
| 公寓類 | 公寓（5F以下無電梯）| 通常屋齡較高，公設比低 |
| 透天類 | 透天厝、連棟透天 | 含土地，總價計算方式不同 |
| 套房類 | 套房（1房1廳1衛）| 坪數小，單價通常較高 |

### 注意事項

- **大樓公設比通常 30–35%**，實際使用坪數（主建物）只有登記坪數的 65–70%
- 比較時建議同時看「含公設單價」與「主建物單價」
- 透天厝含土地價值，單純比較建物單價意義不大

---

## 場景三：查詢預售屋成交資訊

### 適用情境

想知道某地區預售屋的成交均價，比較與成屋的價差。

### 與成屋查詢的差異

| 項目 | 成屋（買賣）| 預售屋 |
|------|------------|--------|
| 資料端點 | `type=buy` | `type=presale` |
| 建築完成年月 | 有 | 無（尚未完工）|
| 交易日期意義 | 實際成交日 | 簽訂契約日 |
| 面積登記 | 已確定 | 預估坪數，完工可能微調 |
| 交屋時間 | 立即 | 通常 2–4 年後 |

### 預售屋特別注意

- 預售屋資料為**簽約時的合約價**，不含後續換約加價
- 部分建商採「分批解盲」，早期低價可能拉高整體均價認知
- 預售房價通常高於周邊成屋 10–20%（反映未來增值預期）

---

## 場景四：租賃行情查詢

### 適用情境

評估某地區的租金水準，計算租金報酬率（租金 / 房價）。

### 租賃查詢特有欄位

| 欄位 | 說明 |
|------|------|
| `每月租金` | 月租金（元）|
| `租賃期間` | 起訖日期 |
| `租賃住宅服務` | 是否為包租代管物件 |
| `有無附傢俱` | 含傢俱或空屋 |
| `電費` | 自付或含在租金內 |
| `水費` | 自付或含在租金內 |
| `管理費` | 自付或含在租金內 |

### 租金報酬率計算

```
年租金報酬率 = （月租金 × 12）÷ 房屋總價 × 100%

範例：
月租金 NT$25,000 × 12 = NT$300,000（年租金）
房屋總價 NT$10,000,000
租金報酬率 = 300,000 ÷ 10,000,000 = 3%
```

> 台灣各地租金報酬率參考：台北市 1.5–2.5%、新北市 2–3%、其他縣市 3–5%

---

## API 呼叫範例

### Python 範例

```python
import requests
import os

API_KEY = os.environ.get("DATA_GOV_API_KEY")

def query_real_estate(county, season, district=None, building_type=None):
    """
    查詢實價登錄買賣成交資料
    
    county: 縣市代碼（如 'A' 為台北市）
    season: 年季（如 '1134' 為民國113年第4季）
    district: 行政區（選用，如 '大安區'）
    building_type: 建物型態（選用）
    """
    url = "https://plvr.land.moi.gov.tw/DownloadOpenData"
    
    params = {
        "type": "buy",
        "county": county,
        "season": season,
        "format": "json",
        "apiKey": API_KEY
    }
    
    response = requests.get(url, params=params, timeout=30)
    response.raise_for_status()
    data = response.json()
    
    # 依行政區篩選
    if district:
        data = [d for d in data if d.get("鄉鎮市區") == district]
    
    # 依建物型態篩選
    if building_type:
        data = [d for d in data if building_type in d.get("建物型態", "")]
    
    return data


def calculate_price_stats(data):
    """計算單價統計（換算為元/坪）"""
    SQMT_TO_PING = 3.30579
    
    prices = []
    for item in data:
        sqmt_price = float(item.get("單價元平方公尺", 0))
        if sqmt_price > 0:
            ping_price = sqmt_price * SQMT_TO_PING
            prices.append(ping_price)
    
    if not prices:
        return {}
    
    prices.sort()
    n = len(prices)
    
    return {
        "筆數": n,
        "平均單價（萬/坪）": round(sum(prices) / n / 10000, 1),
        "中位數單價（萬/坪）": round(prices[n // 2] / 10000, 1),
        "最低單價（萬/坪）": round(prices[0] / 10000, 1),
        "最高單價（萬/坪）": round(prices[-1] / 10000, 1),
    }


# 使用範例
if __name__ == "__main__":
    # 查詢台北市大安區最新一季大樓成交
    data = query_real_estate(
        county="A",
        season="1134",
        district="大安區",
        building_type="住宅大樓"
    )
    stats = calculate_price_stats(data)
    print(f"台北市大安區住宅大樓：{stats}")
```

---

### curl 範例

```bash
# 查詢台北市 113年第4季買賣成交資料
curl -G "https://plvr.land.moi.gov.tw/DownloadOpenData" \
  --data-urlencode "type=buy" \
  --data-urlencode "county=A" \
  --data-urlencode "season=1134" \
  --data-urlencode "format=json" \
  --data-urlencode "apiKey=${DATA_GOV_API_KEY}"
```

---

## 常見問題排除

### 回傳資料筆數為 0

| 原因 | 解決方案 |
|------|----------|
| 年季格式錯誤 | 確認格式為 `YYYQ`（民國年+季別），如 `1134` |
| 縣市代碼錯誤 | 對照 [縣市代碼表](house_api_reference.md) |
| 該季資料尚未釋出 | 每季資料約於季末後一個月公開，查詢最新季前先確認 |
| 行政區名稱錯誤 | 使用完整名稱，如「大安區」而非「大安」 |

### API 回傳 401

| 原因 | 解決方案 |
|------|----------|
| API 金鑰未帶入 | 確認 Header 或參數中有傳入 `apiKey` |
| 金鑰已過期 | 至 data.gov.tw 重新申請或更新金鑰 |

### 單價數值異常（過低或過高）

| 原因 | 解決方案 |
|------|----------|
| 車位單獨交易混入 | 過濾總價 < 50 萬的資料 |
| 特殊關係交易 | 過濾備註欄含「特殊關係」的資料 |
| 整棟買賣 | 過濾備註欄含「整棟」的資料 |

---

## 查詢速查表

```
┌──────────────────────────────────────────────────────────────────┐
│                  查詢情境速查                                        │
├──────────────┬────────────────────┬───────────────────────────────┤
│   情境       │    type 參數        │    重點欄位                    │
├──────────────┼────────────────────┼───────────────────────────────┤
│ 買賣行情     │ buy                │ 單價元平方公尺、建物型態        │
│ 預售屋       │ presale            │ 總價元、建物移轉總面積㎡        │
│ 租賃行情     │ rent               │ 每月租金、租賃期間              │
├──────────────┼────────────────────┼───────────────────────────────┤
│ 新屋（5年內）│ buy + 屋齡篩選     │ 建築完成年月（近 5 年）         │
│ 中古屋       │ buy + 屋齡篩選     │ 建築完成年月（5–20 年）         │
│ 老屋（20年+）│ buy + 屋齡篩選     │ 建築完成年月（超過 20 年）       │
├──────────────┼────────────────────┼───────────────────────────────┤
│ 低樓層       │ buy + 移轉層次篩選  │ 移轉層次（1–3 樓）             │
│ 高樓層       │ buy + 移轉層次篩選  │ 移轉層次（10 樓以上）           │
└──────────────┴────────────────────┴───────────────────────────────┘
```
